#!/usr/bin/env node
// backend/bin/www
//  Starts express application to listen for server req only after authenticating db connection

// Import environment variables
require('dotenv').config();

const { port } = require('../config');

const app = require('../app');
const db = require('../db/models');

